import { MarketMap } from "@/components/market-map";

export const metadata = {
  title: "Market Map | CASCADIAN",
  description: "Navigate prediction markets by category and sentiment using visual heatmaps",
};

export default function MarketMapPage() {
  return <MarketMap />;
}
