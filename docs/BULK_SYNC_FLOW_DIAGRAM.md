# Bulk Wallet Sync System - Flow Diagrams

## Overview Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                    BULK WALLET SYNC SYSTEM                          │
│                                                                     │
│  Problem: Making 1,000+ API calls per wallet = 5 min/wallet        │
│  Solution: Sync ALL trades to ClickHouse, use SQL queries          │
└─────────────────────────────────────────────────────────────────────┘

                              ┌──────────┐
                              │ One-Time │
                              │  Setup   │
                              └────┬─────┘
                                   │
                                   ↓
                        ┌──────────────────┐
                        │ Bulk Sync        │
                        │ (24-48 hours)    │
                        │ 6,605 wallets    │
                        └────────┬─────────┘
                                 │
                                 ↓
                        ┌──────────────────┐
                        │ Calculate        │
                        │ Category Omega   │
                        │ (11 minutes)     │
                        └────────┬─────────┘
                                 │
                                 ↓
                        ┌──────────────────┐
                        │ Incremental Sync │
                        │ (Daily/Hourly)   │
                        │ Cron Jobs        │
                        └──────────────────┘
```

## Detailed Data Flow - Bulk Sync

```
┌─────────────────────────────────────────────────────────────────────┐
│                      BULK SYNC PROCESS                              │
└─────────────────────────────────────────────────────────────────────┘

Step 1: Fetch Wallets
─────────────────────────────────────────────────────────────────────
  ┌──────────────────┐
  │ Supabase         │
  │ wallet_scores    │  SELECT wallet_address FROM wallet_scores
  │                  │  ORDER BY omega_ratio DESC
  │ 6,605 wallets    │
  └────────┬─────────┘
           │
           │ Returns: ['0x742d35Cc...', '0xABCD1234...', ...]
           ↓

Step 2: Batch Processing (50 concurrent wallets)
─────────────────────────────────────────────────────────────────────
  For each batch of 50 wallets:

  ┌──────────────────────────────────────────────────────────────┐
  │ Wallet 1     Wallet 2     Wallet 3    ...    Wallet 50       │
  │    ↓            ↓            ↓                    ↓           │
  │ Goldsky     Goldsky      Goldsky             Goldsky         │
  │ API Call    API Call     API Call            API Call        │
  │    ↓            ↓            ↓                    ↓           │
  │ Process     Process      Process              Process        │
  │    ↓            ↓            ↓                    ↓           │
  └────┼────────────┼────────────┼────────────────────┼───────────┘
       │            │            │                    │
       └────────────┴────────────┴────────────────────┘
                              │
                              ↓
                    ┌──────────────────┐
                    │ Batch Insert to  │
                    │ ClickHouse       │
                    │ trades_raw       │
                    └────────┬─────────┘
                             │
                             ↓
                    ┌──────────────────┐
                    │ Update Metadata  │
                    │ Supabase         │
                    │ sync_metadata    │
                    └────────┬─────────┘
                             │
                             ↓
                    ┌──────────────────┐
                    │ Save Checkpoint  │
                    │ (every 100       │
                    │  wallets)        │
                    └──────────────────┘

Step 3: Progress Tracking
─────────────────────────────────────────────────────────────────────
  Console Output:

  [Batch 152/133] Processing 50 wallets...

  📊 Progress: 1523/6605 (23%)
     ✅ Completed: 1500
     ❌ Failed: 23
     📈 Total trades synced: 342,156
     ⏱️  Elapsed: 4h 12m
     ⏳ ETA: 14h 28m
     💾 Checkpoint saved
```

## Detailed Data Flow - Category Omega Calculation

```
┌─────────────────────────────────────────────────────────────────────┐
│               CATEGORY OMEGA CALCULATION (SQL-BASED)                │
└─────────────────────────────────────────────────────────────────────┘

Step 1: Load Categories (Once)
─────────────────────────────────────────────────────────────────────
  ┌──────────────────┐
  │ Supabase         │
  │ markets table    │  SELECT condition_id, category FROM markets
  │                  │  WHERE category IS NOT NULL
  │ 20,214 markets   │
  └────────┬─────────┘
           │
           │ Returns: Map<condition_id, category>
           │ { '0x1234...': 'Politics', '0x5678...': 'Crypto', ... }
           ↓
  ┌────────────────────┐
  │ In-Memory Cache    │
  │ condition → cat    │
  │ 20,214 entries     │
  └────────────────────┘

Step 2: Query Trades per Wallet
─────────────────────────────────────────────────────────────────────
  For each wallet:

  ┌──────────────────┐
  │ ClickHouse       │
  │ trades_raw       │  SELECT condition_id, side, pnl_net, usd_value
  │                  │  FROM trades_raw
  │ 330K+ trades     │  WHERE wallet_address = '0x742d35Cc...'
  └────────┬─────────┘
           │
           │ Returns: Array of trades with condition_id
           ↓

Step 3: In-Memory Join
─────────────────────────────────────────────────────────────────────
  ┌────────────────────────────────────────┐
  │ For each trade:                        │
  │   category = categoryMap[condition_id] │
  │   Group by category                    │
  │                                        │
  │ Result:                                │
  │ {                                      │
  │   'Politics': [trade1, trade2, ...],   │
  │   'Crypto': [trade3, trade4, ...],     │
  │   'Sports': [trade5, trade6, ...]      │
  │ }                                      │
  └───────────────────┬────────────────────┘
                      │
                      ↓

Step 4: Calculate Metrics per Category
─────────────────────────────────────────────────────────────────────
  For each category:

  ┌────────────────────────────────────────┐
  │ Politics:                              │
  │   total_pnl = sum(pnl_net)             │
  │   total_gains = sum(pnl_net > 0)       │
  │   total_losses = sum(abs(pnl_net < 0)) │
  │   omega_ratio = gains / losses         │
  │   win_rate = wins / total_trades       │
  │   ... (12 metrics)                     │
  └───────────────────┬────────────────────┘
                      │
                      ↓

Step 5: Save to Database
─────────────────────────────────────────────────────────────────────
  ┌──────────────────────────────────────┐
  │ Supabase                             │
  │ wallet_scores_by_category            │
  │                                      │
  │ INSERT:                              │
  │ { wallet_address, category,          │
  │   omega_ratio, total_pnl, ... }      │
  │                                      │
  │ ON CONFLICT (wallet, category)       │
  │ DO UPDATE                            │
  └──────────────────────────────────────┘

Performance:
  - API Approach: 5 minutes per wallet (1,000+ API calls)
  - SQL Approach: 100ms per wallet (1 query + in-memory join)
  - Speedup: 100x faster
```

## Data Model

```
┌─────────────────────────────────────────────────────────────────────┐
│                          DATA MODEL                                 │
└─────────────────────────────────────────────────────────────────────┘

ClickHouse: trades_raw
─────────────────────────────────────────────────────────────────────
┌─────────────────┬──────────────┬──────────────────────────────────┐
│ Column          │ Type         │ Purpose                          │
├─────────────────┼──────────────┼──────────────────────────────────┤
│ trade_id        │ String       │ Unique identifier                │
│ wallet_address  │ String       │ Trader's wallet                  │
│ market_id       │ String       │ Polymarket ID (if found)         │
│ condition_id    │ String       │ ⭐ NEW: For category joins      │
│ timestamp       │ DateTime     │ Trade timestamp                  │
│ side            │ Enum(YES/NO) │ Position side                    │
│ entry_price     │ Decimal      │ Entry price (0-1)                │
│ shares          │ Decimal      │ Number of shares                 │
│ usd_value       │ Decimal      │ Trade value in USD               │
│ pnl_net         │ Decimal      │ Net P&L (with fees)              │
│ ...             │              │                                  │
└─────────────────┴──────────────┴──────────────────────────────────┘

Supabase: markets
─────────────────────────────────────────────────────────────────────
┌─────────────────┬──────────────┬──────────────────────────────────┐
│ Column          │ Type         │ Purpose                          │
├─────────────────┼──────────────┼──────────────────────────────────┤
│ market_id       │ TEXT         │ Polymarket ID                    │
│ condition_id    │ TEXT         │ ⭐ Join key with ClickHouse     │
│ category        │ TEXT         │ Politics, Crypto, Sports, etc.   │
│ title           │ TEXT         │ Market title                     │
│ ...             │              │                                  │
└─────────────────┴──────────────┴──────────────────────────────────┘

Supabase: wallet_sync_metadata
─────────────────────────────────────────────────────────────────────
┌──────────────────────┬──────────────┬──────────────────────────┐
│ Column               │ Type         │ Purpose                  │
├──────────────────────┼──────────────┼──────────────────────────┤
│ wallet_address       │ TEXT         │ Primary key              │
│ sync_status          │ TEXT         │ pending/syncing/complete │
│ total_trades_synced  │ INTEGER      │ Trades in ClickHouse     │
│ last_synced_at       │ TIMESTAMPTZ  │ Last sync time           │
│ last_trade_timestamp │ TIMESTAMPTZ  │ ⭐ For incremental sync │
│ sync_duration_ms     │ INTEGER      │ Performance tracking     │
│ error_count          │ INTEGER      │ Retry logic              │
│ last_error           │ TEXT         │ Debugging                │
└──────────────────────┴──────────────┴──────────────────────────┘

Supabase: wallet_scores_by_category
─────────────────────────────────────────────────────────────────────
┌─────────────────┬──────────────┬──────────────────────────────────┐
│ Column          │ Type         │ Purpose                          │
├─────────────────┼──────────────┼──────────────────────────────────┤
│ wallet_address  │ TEXT         │ Composite key (part 1)           │
│ category        │ TEXT         │ Composite key (part 2)           │
│ omega_ratio     │ DECIMAL      │ gains / losses                   │
│ total_pnl       │ DECIMAL      │ Net profit/loss                  │
│ total_gains     │ DECIMAL      │ Sum of winning trades            │
│ total_losses    │ DECIMAL      │ Sum of losing trades             │
│ win_rate        │ DECIMAL      │ % of winning trades              │
│ roi_per_bet     │ DECIMAL      │ Average profit per trade         │
│ ...             │              │                                  │
└─────────────────┴──────────────┴──────────────────────────────────┘
```

## Key SQL Join

```sql
-- This is the CORE QUERY that makes everything 100x faster

-- ClickHouse → Fetch wallet trades with condition_id
SELECT
  condition_id,
  side,
  pnl_net,
  usd_value
FROM trades_raw
WHERE wallet_address = '0x742d35Cc6634C0532925a3b844Bc454e4438f44e'

-- In-Memory Join (TypeScript)
const categoryMap = new Map<condition_id, category>()
// Load from Supabase: SELECT condition_id, category FROM markets

for (const trade of trades) {
  const category = categoryMap.get(trade.condition_id)
  // Group by category
  // Calculate omega per category
}

-- Result: Category-level metrics in <100ms
```

## Error Handling Flow

```
┌─────────────────────────────────────────────────────────────────────┐
│                     ERROR HANDLING                                  │
└─────────────────────────────────────────────────────────────────────┘

Wallet Sync Attempt
─────────────────────────────────────────────────────────────────────
                ┌─────────────┐
                │ Sync Wallet │
                └──────┬──────┘
                       │
                ┌──────▼──────────┐
                │ API Call        │
                │ (Goldsky)       │
                └──────┬──────────┘
                       │
            ┌──────────▼─────────────┐
            │ Success?               │
            └─────┬──────────┬───────┘
                  │          │
              Yes │          │ No
                  │          │
        ┌─────────▼──┐   ┌──▼──────────────┐
        │ Process    │   │ Update Metadata │
        │ Trades     │   │ status = failed │
        └─────┬──────┘   │ error_count++   │
              │          │ last_error = X  │
        ┌─────▼──────┐   └─────────────────┘
        │ Insert to  │
        │ ClickHouse │
        └─────┬──────┘
              │
        ┌─────▼──────┐
        │ Update     │
        │ Metadata   │
        │ status =   │
        │ completed  │
        └────────────┘

Retry Logic (Bulk Sync)
─────────────────────────────────────────────────────────────────────
  After all batches complete:

  ┌────────────────────┐
  │ Failed Wallets     │
  │ error_count < 3    │
  └─────────┬──────────┘
            │
            ↓
  ┌────────────────────┐
  │ Wait 5 seconds     │
  └─────────┬──────────┘
            │
            ↓
  ┌────────────────────┐
  │ Retry Sync         │
  └─────────┬──────────┘
            │
    ┌───────▼────────┐
    │ Success?       │
    └───┬────────┬───┘
        │        │
    Yes │        │ No
        │        │
   ┌────▼───┐ ┌─▼────────────┐
   │ Mark   │ │ Increment    │
   │ OK     │ │ error_count  │
   └────────┘ └──────────────┘

  After 3 retries → Mark as permanently failed
```

## Checkpoint System

```
┌─────────────────────────────────────────────────────────────────────┐
│                    CHECKPOINT SYSTEM                                │
└─────────────────────────────────────────────────────────────────────┘

Normal Operation
─────────────────────────────────────────────────────────────────────
  Wallet 1 → Wallet 2 → ... → Wallet 100
                               ↓
                        ┌──────────────┐
                        │ Save         │
                        │ Checkpoint   │
                        │              │
                        │ .json file   │
                        └──────────────┘

  Wallet 101 → Wallet 102 → ... → Wallet 200
                                   ↓
                            ┌──────────────┐
                            │ Save         │
                            │ Checkpoint   │
                            └──────────────┘

Interruption (Network, Crash, etc.)
─────────────────────────────────────────────────────────────────────
  Wallet 1 → ... → Wallet 342 → ❌ CRASH

  Checkpoint file contains:
  {
    "totalWallets": 6605,
    "completedWallets": 342,
    "lastProcessedWallet": "0xABC123...",
    "totalTradesSynced": 17560,
    ...
  }

Resume
─────────────────────────────────────────────────────────────────────
  $ npx tsx scripts/sync-all-wallets-bulk.ts --resume

  ┌─────────────────┐
  │ Load Checkpoint │
  │ lastProcessed = │
  │ Wallet 342      │
  └────────┬────────┘
           │
           ↓
  ┌─────────────────┐
  │ Skip wallets    │
  │ 1-342           │
  └────────┬────────┘
           │
           ↓
  ┌─────────────────┐
  │ Resume from     │
  │ Wallet 343      │
  └─────────────────┘

  Wallet 343 → Wallet 344 → ... → Wallet 6605 ✅
```

## Performance Comparison

```
┌─────────────────────────────────────────────────────────────────────┐
│           API APPROACH vs SQL APPROACH                              │
└─────────────────────────────────────────────────────────────────────┘

API Approach (OLD - BROKEN)
─────────────────────────────────────────────────────────────────────
  For each wallet (6,605):
    For each trade (~100):
      API call: tokenId → condition_id     ← 1,000+ calls
      API call: condition_id → market_id   ← 1,000+ calls
      API call: market_id → category       ← 1,000+ calls

  Total time per wallet: 5+ minutes
  Total time for 6,605: 550 hours (23 days)
  Coverage: 2-5% (timeouts)

SQL Approach (NEW - CORRECT)
─────────────────────────────────────────────────────────────────────
  One-time bulk sync (24-48 hours):
    For each wallet (6,605):
      Fetch trades from Goldsky           ← 1 call
      Store with condition_id in ClickHouse

  Category omega calculation:
    Load categories (once): 1 query
    For each wallet:
      Query ClickHouse for trades         ← 1 query
      In-memory join with categories      ← Instant
      Calculate metrics                   ← Instant

  Total time per wallet: 100ms
  Total time for 6,605: 11 minutes
  Coverage: 100%

  Speedup: 100x faster ⚡
```

## Architecture Decision: Why This Works

```
┌─────────────────────────────────────────────────────────────────────┐
│                  WHY THIS ARCHITECTURE SCALES                       │
└─────────────────────────────────────────────────────────────────────┘

Key Insight #1: Store the Join Key
─────────────────────────────────────────────────────────────────────
  ❌ Don't store: tokenId (needs resolution)
  ✅ Do store: condition_id (direct join key)

  Result: No API calls needed for category lookup

Key Insight #2: Load Reference Data Once
─────────────────────────────────────────────────────────────────────
  ❌ Don't: Query Supabase for each trade
  ✅ Do: Load all categories into Map<> at startup

  Result: In-memory join = instant

Key Insight #3: Separate ETL from Analytics
─────────────────────────────────────────────────────────────────────
  ❌ Don't: Calculate metrics during sync
  ✅ Do: Sync trades first, calculate later

  Result: Can re-calculate metrics anytime without re-syncing

Key Insight #4: Use the Right Database
─────────────────────────────────────────────────────────────────────
  Trades (330K+ rows, append-only):
    ClickHouse ← OLAP, time-series, fast aggregations

  Metadata (6,605 rows, updates):
    Supabase ← OLTP, relational, easy queries

  Result: Each database does what it's best at
```

## Summary

This system transforms a broken architecture (API-based) into a scalable, production-ready system (SQL-based) that:

1. ✅ Syncs all trades once to ClickHouse (foundation)
2. ✅ Uses SQL joins instead of API calls (100x faster)
3. ✅ Achieves 100% category coverage (vs 2-5%)
4. ✅ Scales to millions of trades
5. ✅ Provides incremental updates (fresh data)
6. ✅ Enables all 102 metrics from Austin's spec

**Time to value**: 24-48 hours (initial sync) → Ready for production
